services:
  backend:
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  frontend:
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules

  db:
    ports:
      - "5432:5432"
